// Este arquivo assume que o Firebase já foi carregado via CDN no HTML
// e que firebase, firebaseApp e firebaseAnalytics estão disponíveis globalmente.

// Exporte as instâncias globais para uso nos outros módulos
export const app = window.firebaseApp
export const analytics = window.firebaseAnalytics
